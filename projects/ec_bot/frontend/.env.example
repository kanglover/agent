# 前端本地环境变量示例
# 用于配置 API 基址和 Vite 开发代理目标。

# 后端 API 基址。
# 本地开发留空即可：此时请求走相对路径 /api，由 vite.config.ts 的 dev proxy
# 转发到下面的 VITE_DEV_PROXY_TARGET，不存在跨域问题。
#
# Vercel 上必须配置为后端真实地址（在 Vercel 项目的 Environment Variables 里设置），
# 因为 Vercel 与 Render 不同源，前端需要发出完整的跨域请求：
#   VITE_API_BASE_URL=https://ec-bot-api.onrender.com
# 注意：该变量在构建时被写进产物，改完必须重新部署才会生效。
VITE_API_BASE_URL=

# 仅本地开发使用：vite dev server 把 /api 代理到哪个后端
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
